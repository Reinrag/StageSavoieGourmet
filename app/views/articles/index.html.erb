<h1 align="center">Liste des articles</h1>
<p><%= link_to "Créer un nouvle article", new_article_path, class:"btn btn-default" %></p>
</br>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Titre</th>
            <th>Description</th>
        </tr>
    </thead>  
        <% @articles.each do |article| %>
    <tbody>   
        <tr> 
            <td> <%= article.titre %> </td>
            <td> <%= article.description %> </td>
            <td> <%= link_to "Lire l'article", article_path(article), class:"btn btn-info" %> </td>
            <td> <%= link_to "Effacer l'article", article_path(article), method: :delete, class:"btn btn-danger" %> </td>
        </tr>
        
        <% end %>
    </tbody> 
</table>