<!DOCTYPE html>
<html>
  <head>
    <title>SavoieGourmet</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="navbar navbar-inverse">
      <div class="container">
        <div class="navbar-header">
          <a href="<%= root_path %>" class="navbar-brand">Savoie Gourmet</a>
        </div>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Articles", articles_path %></li>
          <% if logged_in? %>
          <li><%= link_to "Se déconnecter", logout_path, method: :delete %></li>
          <% else %>
          <li><a href="<%= signup_path %>">S'inscrire</a></li>
          <li><%= link_to "Se connecter", login_path %></li>
          <% end %>
        </ul>
      </div>
    </div>
    <%= render 'layouts/messages' %>
    <%= yield %>
  </body>
</html>
